import 'package:flutter/material.dart';

class CustomListaContenedores extends StatefulWidget{

  CustomListaContenedores({
    Key ? key,
  }): super (key: key);

  @override
  State<CustomListaContenedores> createState() => _CustomListaContenedoresState();
}

class _CustomListaContenedoresState extends State<CustomListaContenedores> with SingleTickerProviderStateMixin{

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        
      ],
    );
  }
}