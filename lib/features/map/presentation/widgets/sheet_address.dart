import 'package:flutter/material.dart';

class SheetAddress extends StatelessWidget {
  // TODO: agregar propiedad, direccion, distanciad del usuario, zona, contenedores cercanos
  SheetAddress({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Stack();
  }
}